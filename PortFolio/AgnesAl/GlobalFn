function mytemplate() {


//Use hideCol instead!!


//during plotting data
  //If found Filter, must get copied, remove
  //applied everything
  //paste back copied filter
//Fn involved
  //copFil
  //


//once edit, submit and manual trigger
  //copy before insert Row (copFil fn )
  //insert row (optional) and write data
  //if can copy , copied the value and applied again (refresh fil)
  // gtFil = (sheetname) => SpreadsheetApp.getActiveSpreadsheet().getSheetByName(sheetname).getFilter();


// copFil() -> plotting data ->  refFil()



}
